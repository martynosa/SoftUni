let domElement = document.querySelector('the main element');
let NodeElement = ['querySelectorAll'];
function sort(){
    Array.from(NodeElements)
    .sort((a,b) => a.textContent.localeCompare(b.textContent))
    .forEach(el => domElement.appendChild(g));
};